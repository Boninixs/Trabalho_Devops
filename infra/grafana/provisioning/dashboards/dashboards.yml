apiVersion: 1

providers:
  - name: item-service
    orgId: 1
    folder: Item Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
