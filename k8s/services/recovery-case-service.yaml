apiVersion: v1
kind: Service
metadata:
  name: recovery-case-service
  namespace: devops
spec:
  selector:
    app: recovery-case-service
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP