apiVersion: v1
kind: Service
metadata:
  name: recovery-postgres-service
  namespace: devops
spec:
  selector:
    app: recovery-postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP