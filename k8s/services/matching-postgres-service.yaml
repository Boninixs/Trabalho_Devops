apiVersion: v1
kind: Service
metadata:
  name: matching-postgres-service
  namespace: devops
spec:
  selector:
    app: matching-postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP

  