[pytest]
testpaths = tests
markers =
    integration: tests that require a PostgreSQL database
    contract: contract tests for match event envelopes
    idempotency: tests for event reprocessing and duplicate prevention
